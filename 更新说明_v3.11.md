 # 更新说明（v2.4 → v3.12.3）

## v3.12.3 金额口径统一：服务费"分位取整" + 「剩余备款」不加清零（09-09）

> 起因：同一个「消耗+服务费」在界面上出现 **$747.48 与 $747.45 两个数**；同一个「净备款」出现 **$4,352.52 与 $4,252.52 两个数**。两件事都查清了根因并全站统一。

### 一、为什么会有 747.45 / 747.48 两个数（排查结论）
两套**服务费取整口径**长期并存：

| 口径 | 做法 | 服务费 | 消耗+服务费 | 用在哪些地方 |
|---|---|---|---|---|
| ① 原始浮点 | 全程不取整累加，显示时才 round | **$21.77** | **$747.48** | 后端 summary、Hero「累计消耗+服务费」、指标卡「服务费」、按账号汇总 Σ、按客户汇总 Σ、备款明细 Σ、打款管理顶部卡、备款一览、Excel 导出 |
| ② 逐行分位 | **每条消耗记录的服务费先四舍五入到分**，再累加 | **$21.74** | **$747.45** | 媒体×渠道 Σ、客户×渠道 Σ、按日期汇总 Σ（2026-09-05 起就写明"分位取整后可加"） |

差值来源：本库单笔服务费量级极小（0.0084 / 0.0015 / 0.0003…），**100 条**记录逐行取整的累计漂移 = **$0.03**。
更麻烦的是还有第三、第四个数："按账户分组、组内原始值再加总" = **$21.79**，"按日期分组" 同样 = **$21.79** —— 换成别的维度又能冒出新的分位差。
另外还查出一处**导出与界面不一致**：Excel「按日期汇总」sheet 的`备款`列公式写成 `打款 − 消耗 − 服务费 **+ 充值**`，导出值 $7,052.52 vs 界面 $4,252.55，差的正好是一整笔充值 $2,800。

**定稿口径（全站唯一）**：服务费在「每条 BM 消耗记录」这一层就四舍五入到分，之后所有聚合（按日 / 按账户 / 按客户 / 按渠道 / 全量 / Excel 导出）都在这个分位值上累加。
- 实现：前端新增 `feeCent(spend, rate)`，后端新增 `fee_cent()` / `row_fee_cent()`（用 `Decimal + ROUND_HALF_UP`，与 JS `Math.round(x*100)/100` 同行为，避开 Python `round()` 的银行家舍入）
- 好处：**任何维度的 Σ 恒等于全体明细之和，也恒等于你把表格里看得见的数字逐行加起来的结果**（示例账户：界面行服务费 $1.78 = 弹窗 6 行逐行相加 $1.78 = 弹窗 Σ $1.78）
- 落点：按客户汇总、备款明细（日级 + 客户级两处）、备款一览 sMap、按日期汇总的「备款金额」running、Excel 全部 sheet、后端 `/api/stats`、`/api/consume/stats`（cl_agg/ch_agg/daily/balances/total_fee/consume_all_fee）、`/api/consume/account/*`（弹窗 fee 由 4 位小数改 2 位）
- 媒体×渠道 / 客户×渠道 本来就是分位口径，数值不变

### 二、净备款统一为「🏦 剩余备款」，**不加清零**（按你的定稿）
排查发现同一口径曾有 **3 个变体**在跑，且注释与代码互相打架：

| 位置 | 修复前公式 | 修复前值 | 修复后 |
|---|---|---|---|
| 数据概览 Hero「🏦 剩余备款」 | 打款 − 消服 | $4,252.52 | **$4,252.55**（分位口径） |
| 打款管理顶部卡「🏦 **净备款**」 | 打款 − 消服 **+ 清零** | **$4,352.52** | 改名「🏦 **剩余备款**」，去掉 + 清零 → **$4,252.55** |
| 备款一览「合计」行 | 打款 − 消服 **+ 清零** | **$4,352.52**（与同表明细行 $4,252.52 自相矛盾） | 去掉 + 清零 → **$4,252.55**，与明细行一致 |
| 备款一览 明细行 | 打款 − 消服 | $4,252.52 | $4,252.55 |
| 备款明细「累计**净**备款」 | 打款 − 消服 | $4,252.52 | 改名「累计**剩余**备款」→ $4,252.55 |
| 按日期汇总「备款金额」 | 打款 − 消服 | $4,252.52 | $4,252.55 |
| 充值明细「客户**净**备款」 | 打款 − 消服 **− 退款**（表头 title 却写"+ 累计清零"，代码/文案双重不一致） | 与上面都不同 | 改名「客户**剩余**备款」，公式统一为 打款 − 消服 → **$4,252.55** |
| Excel「备款一览」sheet | 列名`净备款` | — | 列名改`剩余备款`，值 $4,252.55 |
| Excel「按日期汇总」sheet | 备款 = 打款 − 消服 **+ 充值** | $7,052.52 | 去掉 + 充值 → $4,252.55 |

**全站唯一公式：🏦 剩余备款 = 💰 累计打款 − 📈 累计(消耗 + 服务费)**；充值 / 清零 / 退款 一律只作流水展示列，不参与计算。
（退款管理页的「**可退备款**」是另一个语义 = 剩余备款 − 已退款，名称与用途都不同，**保持原样未动**。）
所有相关列头 title、页面底部的「💡 备款口径」说明、代码注释（含 `renderCli` / `renderPay` / `refreshConsume` 里 3 处写着"+ 清零"的过期注释）已同步改写，避免下次再被文案带偏。

### 三、实测（本版数据：打款 $5,000 / 消耗 $725.71 / 服务费 $21.74 / 清零 $100）
统一到 **服务费 $21.74、消耗+服务费 $747.45、剩余备款 $4,252.55**，以下 12 处已逐一核对为同一个数：
Hero「剩余备款」· Hero「累计消耗+服务费」$747.45 · 指标卡「服务费」$21.74 · 按账号汇总 Σ · 按客户汇总 Σ · 媒体×渠道 Σ · 客户×渠道 Σ · 按日期汇总 Σ 与「备款金额」 · 备款明细 Σ 与「累计剩余备款」 · 打款管理顶部「🏦 剩余备款」 · 备款一览「合计」行与明细行 · 充值明细「客户剩余备款」合计 · Excel 5 个相关 sheet（含按账号汇总 Σ服务费 $21.74）
区间口径同样自洽：08-16~08-20 → 消服 $227.16 = 消耗 $220.54 + 服务费 $6.62，剩余备款 $4,772.84 = 打款 $5,000 − $227.16；账户行服务费 $0.81 == 弹窗 Σ $0.81。
浏览器控制台 **0 报错**；上一轮（v3.12.2）的区间查询/日消耗弹窗/空态修复全部回归通过。

## v3.12.2 数据概览「日消耗表」+「日期区间查询」修复（09-09，基于 19.27.51 新版）

> 本轮只修 数据概览（📊）的 日消耗明细 与 区间查询，**09-09 定稿的备款口径一行未改**。
> 你新版里已自带「按日期汇总 充值列」的修复，本轮不重复处理。

### A. 日消耗表（📊 日消耗弹窗 / 按账号汇总）
| # | 现象（修复前） | 修复后 |
|---|---|---|
| 1 | 设了区间再点「📊 日消耗」，弹窗仍显示**全量**：账户行消耗 $26.97，弹窗 Σ 合计 $34.91，两个数字对不上（区间控件 title 承诺过"作用于…日消耗明细"） | 弹窗打开即**继承指挥台区间**（预填 日期从/到）→ Σ 合计 == 该行消耗金额；标注「继承指挥台「📅 区间查询」」；新增口径说明条（下表=区间口径，上方 📅天数/💰累计消耗/💵账号余额=账户全量口径）；「⟳ 重置」仍可一键回全量 |
| 2 | 导入时日期列为空 / 是「合计」这类非日期文本的行也会入库 → 写成 `date=''` 的消耗行：日消耗明细末尾多出空白日期 $0 行、「📅 天数」虚高、叠加区间后行数跳变（旧版库里曾积压 18 条） | ①导入层：无有效日期的行**不入库**；②读取层：`/api/consume`、`/api/consume/account/*`、`/api/stats`、聚合统计统一过滤历史遗留空日期行（**只读时过滤，不动你的磁盘数据**）；③弹窗渲染再兜底过滤一次。实测导入 4 行（1 正常 / 1 空日期 / 1「合计」/ 1 未下户）→ `新增 1 / 拒绝 1 / 跳过 2`，库内空日期行 0 |
| 3 | 导入口径混淆：缺日期的行被算进「⚠️ 拒绝（广告ID未在下户表登记）」→ 排查方向被带偏 | 拆成两类分别计数与提示：`拒绝 N 行（未下户）` / `跳过 N 行（缺日期）`，实时日志、rawMsg 汇总、导入历史卡片三处同步 |
| 4 | 区间内该账户无消耗时弹窗只剩一句"该区间暂无消耗数据" | 空态写明当前生效的**两组**条件（日期区间 + 更新日期区间）、账户实有天数，并提示区间继承来源；空态判断改用过滤掉幽灵行后的真实行 |
| 5 | 账户ID 含特殊字符时，弹窗日期筛选/重置永远显示「🔄 请刷新数据」 | onclick 传的是 `encodeURIComponent(ID)`、缓存存原始 ID → 比较前统一解码 |

### B. 日期区间查询（📅 区间查询）
| # | 现象（修复前） | 修复后 |
|---|---|---|
| 6 | 日期填反（从 08-20、到 08-16）→ 顶部卡全 $0.00、账户表只剩一行 0 值 Σ、5 张透视表集体空表，**零提示**，像数据被清空 | 自动交换 + toast「⚠️ 起始日期晚于结束日期，已自动交换为 A ~ B」，输入框同步纠正（与弹窗内既有保护同一套行为） |
| 7 | 按账号汇总筛空后只显示「Σ 总计（共 0 个账户）」一行 0 值，**空态永远不出现** | 根因是 `Σ行 + rows.join('') \|\| '空态'` —— JS 里 `+` 优先级高于 `\|\|`，左侧恒为真。已拆开；无命中只输出空态行，`colspan` 15 → 修正为实际 **16 列**，计数条显示「共 0 个账户（当前区间无数据）」 |
| 8 | 其余 5 张透视表筛空后一律写「暂无数据（**请先导入** …）」→ 明明导入过，用户以为导入失败 | 新增 `emptyByRange()`：带区间 → 显示当前区间 + 下一步（放宽区间 / 点「全部」/ 查表头 🔽）；无区间 → 保留原导入引导文案 |
| 9 | 维度结构 4 卡口径打架：区间无数据时 账户数=0 但 客户数/渠道数/媒体数 仍是 1 | 有区间 → 四卡全部按区间内实际有业务的维度计数（title 标明"区间口径"）；无区间 → 维持下户表全量口径，首屏数字不变 |
| 10 | 备款明细 Σ 行用 `colspan="3"` 把「日期+客户+**打款金额**」一起吞掉：`grandPay` 算了从不显示，Σ 第一个数字其实是退款合计。**09-09 新口径下 净备款 = 打款 − 消耗 − 服务费，主项被藏**更说不通 | Σ 行改 `colspan="2"` 并补打款合计 → 8 列逐列对齐明细（实测：打款 $5,000 / 退款 $0 / 消耗 $220.54 / 服务费 $6.62 / 单日合计 $227.16 / 净备款 $4,772.84） |
| 11 | 主区间两个日期框**回车无反应**（本版弹窗 4 个日期框 + 账户表更新日期 2 个框都支持回车，唯独主区间不支持），必须用鼠标点「🔍 查询」 | 回车即查询；近7天 / 本月 / 全部 三个快捷键行为不变 |

### 回归实测（本版数据：消耗 100 行 08-12~08-24 / 充值 24 / 打款 1 / 下户 21）
- 不设区间基线**与修复前完全一致**：消耗 $725.71、充值 $2,800.00、21 个账户、22 行
- 区间 08-16~08-20：消耗 $220.54、11 个账户；账户行 $26.97 == 弹窗 Σ $26.97；备款明细 Σ 8 列自洽（$5,000 − $220.54 − $6.62 = $4,772.84）
- 反序区间：自动交换为 08-16~08-20 并 toast，结果与正序一致；区间日期框回车即生效
- 空区间 01-01~01-05：6 张表全部给出区间空态（按账号汇总 colspan=16），4 张维度卡统一归 0
- 9 个模块全部渲染正常；**浏览器控制台 0 报错**；`data/*.json` 与原包逐字段比对一致（仅登录产生的 sessions 已还原）

### ⚠️ 本轮顺带发现的口径冲突 → 已在 v3.12.3 处理完毕
当时记录：同一个「净备款」两处差 **$100（= 库里唯一一笔清零）**
- 打款管理页顶部：净备款 **$4,352.52** = 打款 5,000 − 消服 747.48 **+ 清零 100**
- 数据概览 Hero「剩余备款」/ 备款明细「累计净备款」：**$4,252.52** = 打款 − 消服（**不含清零**）

且「累计净备款」列 title 写的是"…+ 累计清零"，**文案与代码互相打架**；深挖后又发现第三种变体（充值明细「客户净备款」实际是"− 累计退款"）与第四种（Excel「按日期汇总」备款列"+ 充值"）。
→ 你的定稿：**不加清零、名称统一叫「剩余备款」**，详见上方 v3.12.3（同时一并解决了 $747.45 / $747.48 的服务费取整分歧）。


## v3.11 权限中心新增「导出数据」可选权限
- 权限中心编辑弹窗新增 **「📤 允许导出数据」** 复选框（默认跟随角色：公司人员/客服/财务/管理员=允许，客户=禁止；勾选或取消后**按账号生效**）
- 账号列表新增「导出」列（📤 允许 / 跟随角色 / 🚫 禁止）
- 无权限账号：侧边栏「导出全部 Excel」隐藏、各模块导出按钮隐藏、直接调接口返回 403（后端强校验）
- 后端 `/api/export` 与权限载荷（登录/me）统一改用「角色默认 + 账号覆盖」的有效权限计算


## v3.10 一键安装环境（小白可用）
- 新增 **`安装环境.bat`（Windows）** 与 **`安装环境.command`（macOS）**：双击即自动完成
  ① 安装 Python（winget → 官网安装包兜底，静默安装并加入 PATH；Mac 走 Homebrew 或苹果命令行工具，失败自动开官网下载页）
  ② 安装 Excel 组件 openpyxl（默认源失败自动切清华镜像）
  ③ 直接启动系统并打开浏览器
- `启动.bat` / `start.command` 升级为**自愈启动**：检测到没有 Python 时自动调用一键安装，装完直接拉起系统
- 新增 `!先看这里-安装说明.txt`：面向第一次使用者的 30 秒上手说明（含 Mac「无法打开」的处理、端口占用、改密提醒）
- openpyxl 仅影响 Excel 批量导入；其余功能始终零依赖


## v3.9 状态可行内修改 + 两个表头客户筛选修复
| 项 | 变化 |
|---|---|
| 充值清零 → 充值明细 | 「状态」列由只读文本改为**行内下拉**（🟢正常 / 🟡挂户 / ⚪清零，交互对齐下户表的行内修改）。改动**写回该充值日期命中的下户归属段**（与录入表单的状态下拉同一条通路），充值流水本身**不冗余存 status**；成功后 toast 注明写回的客户归属段并刷新全部派生视图（按账号汇总的对账状态徽标、账号对账状态列同步变化）。 |
| 未登记归属的账户 | 改状态会被后端拒绝并给出可执行的中文提示：`该账户在充值日期 YYYY-MM-DD 没有归属段，请先在「下户表」登记归属`；前端失败后自动回读真实值，避免下拉显示与后端不一致。 |
| 打款管理 | 表头「客户🔽」此前只填充选项、不参与过滤（点了像失灵）。现改为 `applyPaymClientFromHeader()`：与筛选条客户下拉双向同步，**打款明细 + 备款余额两张子表一起过滤**，结果计数胶囊同步（如 `打款 2 / 9 条 · 备款 1 / 8 个客户`）。 |
| 退款管理 | 表头「客户🔽」同样是死筛选，现已生效；**「财务影响合计」改为按筛选结果求和**，保证合计 = 可见行之和；筛到无记录的客户时显示`🔒 客户「×」暂无退款记录`而不是残留合计行。 |
| 修复中带回的死锁隐患 | 抽取共用的段状态写入助手时，最初版本在已持有 `_lock` 的 PUT 里又 `with _lock`（`threading.Lock` 不可重入）→ 自死锁。已改为「调用方持锁」的 `_apply_account_status_locked()`，POST 侧自行加锁；复测通过、无请求挂起。 |

> 转户/口径背景见 v3.8 附录；四个"余额/备款"公式定义在 README 第 4 条与《数据库设计.md》§10。


> 面向使用者的一页变更清单；验证方法与环境见《审计底稿_全链路数据测试.md》。

## v3.8 安全加固（本轮排查后修复）
| # | 问题 | 修复 |
|---|---|---|
| 1 | 存储型 XSS：客户名/账户ID/账号名/渠道/备注（来自 Excel 与录入）在表格与下拉 `<option>` 未转义，实测可执行脚本 | 全站输出转义（含 fillSelect/htmlize/toolbar 下拉/媒体下拉），新增 `escJ()` 处理内嵌 JS 参数。复测：注入元素 0、onerror 未执行，payload 以文本原样显示 |
| 2 | 请求体无上限 → 内存 DoS | 16MB 上限 + `413` 并断连（顺带防请求走私）|
| 3 | PUT 可注入任意字段（伪造 createdBy / attachments / 改 id） | payment/recharge/mapping 字段白名单 |
| 4 | 同文件并发写/断电可能留半截 JSON | `save_json` 唯一临时名 + 写锁 + fsync |
| 5 | `/api/auth/roles` 未登录可读权限矩阵 | 需登录 |
| 6 | 会话表无界增长 | 登录时回收过期项 + 上限 500 |
| 7 | 接口异常无兜底（堆栈半截响应） | do_GET/POST/PUT/DELETE 统一 wrapper → 500 JSON |
| 8 | gzip 响应缺协商维度 | 静态与 JSON 加 `Vary: Accept-Encoding`；凭证图加 `Vary: Cookie` |
| 9 | **邮箱自动备份一直失败**：中文附件名触发 `quote() doesn't support 'encoding' for bytes` | 附件名改 ASCII，包内说明保留中文 |
| 10 | **转账凭证不在备份/恢复内**（换机即丢） | 备份纳入 `uploads/**`，恢复支持图片写回并校验包内路径 |
| 11 | 恢复包路径穿越 | 拒绝 `../`、反斜杠、绝对路径与非白名单文件（含 `uploads/` 内穿越）|

## v3.7 下拉与选项转义根治（并入 v3.8 验收）
`fillSelect` / `htmlize` / 各 toolbar 下拉此前只替换双引号，是 XSS 主入口。

## v3.6 口径命名统一（原先三处都叫"余额"，易误读）
- 首页指标卡 💎「总余额」→ **充值净结余**；日消耗弹窗「当前余额」→ **充值净结余**，并补漏减的服务费（此前与首页口径不一致）
- 按账号汇总「余额」→ **账户净备款**；打款单日汇总「余额」→ **累计净备款**
- 账号对账「账号余额」→ **投放结余**（累计充值−累计消耗），新增 **差额**（投放结余−累计清零）
- 充值明细「账号余额」→ **账户结余**（累计充值−累计清零），末列 **客户净备款**
- 所有列头挂 `title` 公式提示；导出 Excel 列名同步

## v3.5 筛选条统一放到表头正上方
- **打款管理**：新增搜索（客户/账号/备注）+ 账号下拉 + 客户下拉 + 日期区间 + 结果计数 + ⟲重置，一条同时驱动「打款明细」和「备款余额」；选账号自动映射其所属客户
- **充值清零**：筛选条移到表头正上方，同时驱动「充值明细」与「账号对账」
- 新增记录后自动清筛选并高亮定位；计数胶囊改为读取真实渲染行数

## v3.4 账号对账（原「账户余额汇总」）重建
- 改名 **账号对账**；列：客户｜媒体｜渠道｜账号名称｜账户ID｜**状态**｜累计充值｜累计清零｜累计消耗｜投放结余｜差额；去掉服务费、服务点
- 客户/媒体/渠道/状态四个表头 🔽 全可筛选，与账号筛选条叠乘
- 修两个存量 bug：该表**从未进主渲染流程**（打开时是空表/旧数据）；表头客户筛选被工具条反向覆盖导致选择被吞

## v3.3 充值清零「账号筛选」（交互对齐下户表）
账号下拉（显示「账号名称（账户ID） · 客户」）+ 模糊搜索 + 结果计数 + 重置；两张子表共用。

## v3.2 / v3.1 转账凭证与删除权限
- 打款、退款每行右侧「📎」上传转账截图（多选/拖拽、文件头校验、≤8MB、≤12 张），新增表单可先选图后自动挂账；删除凭证与删除记录同权限
- 打款/退款记录删除仅限**管理员 + 财务**（`can_delete_money`，后端强校验）；删除即金额回退（派生值全部按剩余记录重算）
- 退款**不再终止客户**：可反复退款；「可退备款」扣减已退现金；删除旧退款可撤销历史「已终止」标记

## v3.0 / v2.7~v2.9
- 应用内确认/输入弹窗替代原生 `confirm()/prompt()`（原生弹窗在 iframe 会被浏览器静默屏蔽 → 曾表现为"点保存没反应"）；覆盖退款保存、各表删除、清空打款、恢复备份、一键清空双确认、未匹配账户补录
- 面板结构修复：误多出的 `</div>` 导致打款/退款面板变成 body 子元素 → 定位记录时整页滚动把侧栏顶出视口；三档宽度回归 + 小屏导航吸附换行（原 `display:none` 直接隐藏了整个导航）
- 汇总/矩阵当日总计修复为整日全部渠道求和，并统一分位取整（表内可加）；按日期汇总新增「合计」列；充值/凭证导出与界面同口径

## 待你定调的两项
1. `script-src 'unsafe-inline'` 收紧（需改事件委托 + nonce，工作量较大）
2. 备份包是否默认**排除 `users.json`**（当前包含口令哈希，不含明文与会话令牌）

## v3.11.1 一键清空简化（用户选择）
- 取消「键入 DELETE」关键词要求：引导弹窗内**只复核登录密码**（错误/空密码一律 403，实测通过），降低使用门槛
- 引导弹窗、自动安全快照、保留账号/设置/日志等安全设计不变
- 服务端同步修订：`/api/data/wipe` 不再校验 keyword

## v3.11.2 一键清空弹窗打不开的修复（实测发现）
- 现象：点「一键清空业务数据…」按钮后弹窗不出现（就是"没有实现"的直接原因）
- 根因：取消 DELETE 输入框时，`openWipeModal()` 内残留 `wpKey.value=''` 一行 → 抛 TypeError，弹窗未及显示
- 修复后端到端复测：按钮 → 弹窗打开 → 填密码 → 最终确认 → 清空执行（数据归零）→ 从自动快照完整恢复 ✓

## v3.12 数据维护区重构：「备份恢复」「清除数据」双按钮 + 各自独立浮窗
- 原先共享的页面密码框取消；两个高危操作各自弹出**独立浮窗**，在浮窗内选文件/输入登录密码后执行（密码不再停留在页面上）
- 备份恢复浮窗：选 zip → 输密码 → 开始恢复（覆盖前自动安全快照；支持凭证图片随包恢复）
- 清除数据浮窗：输密码 → 最终确认 → 清空（自动快照；取消 DELETE 关键词，v3.11.1）
- 顺带修复：下拉选项/表格所有自由文本输出统一转义（含 `<option>`），杜绝存储型 XSS

## v3.12.1 充值表单新增「客户（自动）」展示
- 添加记录表单在「账号名称」右侧新增只读的**客户（自动）**字段：按「账号ID + 充值日期」命中下户归属段显示客户，**转户账户随充值日期自动切换客户**（如 ACC-TRANSFER-1：8/15→乙公司、8/25→甲公司），悬停显示归属段/媒体/渠道/服务点
- 未下户账户显示「⚠️ 未匹配」并提示先到下户表登记
- 保存时的客户取值与展示**同源**（不再取"该账户第一个段"的客户，转户账户保存到新日期段时归属不会再错挂旧客户）
- 未选充值日期时默认按当天归属显示
