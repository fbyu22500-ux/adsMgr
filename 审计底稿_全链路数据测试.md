# FB 广告账户管理系统 · 全链路数据审计底稿

- 审计日期：2026-09-04
- 审计方式：虚构数据全流程实测（下户表 → 充值 → 打款 → BM 消耗导入 → 退款），全部走真实 API，独立手工复算核对
- 测试数据：3 客户（甲/乙/丙）、5 账户 + 双段账户 ACC-M1 + 牺牲户 Z1 + 幽灵户 X9、6 段归属、5 笔充值、2 笔打款、11 行 BM 数据、1 笔退款
- 核对结果：**33 项核对，31 项通过，2 项不符（同一根因）；审计发现 8 项（高 4 / 中 3 / 低 1）**
- 说明：审计结束已将现场数据完整还原（consume 70 / recharge 79），回归冒烟 31/31 通过

## 一、数据链验证结论（通过的部分）

| 环节 | 验证点 | 结论 |
|---|---|---|
| 下户表 → 消耗导入 | 未下户账户 ID 拒绝入库，返回拒绝清单（ACC-D1/ACC-X9） | ✅ 按规则拒绝 |
| 归属段匹配 | 段首日/末日闭区间命中（A1@0901、A2@0905/0910）、跨段换客户换费率（M1@0903→甲2%、M1@0908→乙4%） | ✅ 行级归属正确 |
| 充值 → 余额 | 充值/清零累计、账户剩余余额计算 | ✅ |
| 打款分摊 | 客户级打款按（消耗+服务费）占比分摊到账户；赊账负备款正常显示 | ✅（自洽） |
| 重复导入 | 同 (date, accountId) 二次导入走更新而非新增（9 行 updated） | ✅ |
| 越权 | 未登录写操作 401、伪造 token 401 | ✅ |

## 二、审计发现（按严重度）

### #2【高】POST /api/mapping 单段覆盖：删除同账户全部历史归属段
- 现象：先导入 Z1 段1（09-01 起），再 POST 段2（09-02 起）→ 段1 被静默删除。server.py:995 `mapping = [m for m in mapping if accountId != aid]`
- 影响：手动"添加记录"与导入中心逐行 POST 通道每次都会清掉该账户其他段；多段区间归属在这两条通道上失效。而 multipart 导入通道（/api/mapping/import）是多段安全的 → 三条通道行为不一致
- 建议：POST 改为按 (accountId, date) 幂等 upsert

### #5【高】多段账户的账户级服务费 = 全段消耗 × 今日段费率（费率错配）
- 现象：ACC-M1 行级服务费真值 12.8（120×2% + 260×4%），账户级 balances.fee=7.6（380×今日段2%），少计 5.2；若今日落在 4% 段则多计
- 影响：账户表、备款、打款分摊的金额全部失真（金额级 bug，本例误差 5.2/12.8 ≈ 41%）
- 建议：账户级 fee 按行级 (spend×段费率) 汇总

### #4【高】多段跨客户账户：账户级归属单 .client 与行级矛盾
- 现象：ACC-M1 行级 0903→甲 / 0908→乙，账户级 balances 全额记 client=甲公司（含乙的 260 消耗）
- 影响：打款分摊、账户备款、账户表客户筛选按账户级客户计算 → 跨段账户财务归属失真
- 建议：balances 按 (accountId+段client) 拆行，或 UI 明示多段归属

### #7【高】退款与客户终止状态无联动
- 现象：POST /api/refund 只追加凭证；client_status.json 无任何写入链路。前端依赖 status=refunded 标记"客户终止"，但该状态永远不会再产生（历史快照数据来自已废弃通道）
- 建议：退款时 upsert client_status，或在退款管理提供"终止合作"动作

### #1【中】下户表导入不读取「状态」列
- 现象：模板有「状态」列（示例表实测），导入后 status 被静默丢弃，全部回退"正常"
- 建议：_handle_mapping_import 增加 status 映射

### #3【中】归属区间外的消耗被"尾段兜底"静默吸收
- 现象：A2 归属止于 09-10，其 09-11 消耗仍导入并归属尾段（防"忘记续期"设计），但与"未下户即拒绝"严格口径不一致，界面无任何超期提示
- 建议：超期行打标记并在账户表提示续期，或提供"拒绝/兜底"开关

### #8【中】备款口径未扣退款，且与代码注释自相矛盾
- 现象：实现口径 备款=打款−(消耗+服务费)，退款不扣；stats 注释却写"客户净备款=…−退款"。丙退 50 后账户备款不变
- 建议：业务确认唯一口径，前后端对齐

### #6【低】未下户账户充值后成为"幽灵账户"混入账户汇总表
- 现象：ACC-X9 无下户仅充值 100 → balances 出现该账户（软规则放行的副作用）
- 建议：加"未下户"徽标或硬拒绝

## 三、复算底表（模拟数据期望值）

| 指标 | 期望 | 实际 | 结论 |
|---|---|---|---|
| BM 导入 11 行 → 新增/拒绝 | 9 / 2（D1,X9） | 9 / 2 | ✅ |
| 重复导入 | 新增0/更新9 | 0/9 | ✅ |
| Δ总消耗 | +1190 | +1190 | ✅ |
| Δ总服务费（行级口径） | +36.5 | +36.5 | ✅ |
| 客户行级 甲/乙/丙 消耗+费 | 557.4 / 576.4 / 92.7 | 一致 | ✅ |
| M1 账户级服务费 | 12.8 | 7.6 | ❌ → #5 |
| Δ打款/充值总额 | +3300 / +3900 | 一致 | ✅ |
| 甲公司三账户赊账备款 | 负数 | -196.31 / -80.05 / -246.24 | ✅（按系统口径自洽）|
| 丙退 50 后 C1 备款 | -92.7（不扣退款） | -92.7 | 记录 → #8 |

## 四、修复优先级建议

1. **先修 #5（费率错配）+ #4（客户归属）**：金额与归属直接错误，影响对账
2. **再修 #2（单段覆盖）**：日常添加/导入通道随时会静默删数据
3. **#7/#8 一起定口径**：退款到底要不要扣备款、要不要联动终止状态
4. #1/#3/#6 属于提示与标记类，随手补

## 五、修复验证（2026-09-04 同日完成）

全部 8 项已修复并回归验证：**38/38 核对通过，剩余发现 0**。

| # | 修复内容 | 验证证据 |
|---|---|---|
| #5 | 账户级服务费改行级汇总（spend×各自行费率） | M1拆行后 甲段2.4 + 乙段10.4 = 12.8 ✅ |
| #4 | balances 按 (账户×归属段客户) 拆行，打款分摊随客户池正确 | M1乙段备款 1136.96 = 乙池分摊 ✅；账户表 M1 呈两行（甲/乙）|
| #2 | POST /api/mapping 改按 (accountId,date) 幂等 upsert | Z1 POST 段2 后保留 2 段 ✅ |
| #7 | 现金退款自动 upsert client_status=refunded（含退款单号/操作人） | 丙公司退 50 后状态联动 ✅ |
| #1 | 下户导入读取「状态」列 | A2 导入后 status=挂户 ✅ |
| #3 | 区间外消耗打 overdue 标记：导入响应返回超期数；日消耗弹窗提示"超出归属截止日 N 天" | 导入 overdue=1 ✅；A2 弹窗警示 ✅ |
| #6 | 无下户的充值账户在账户表带 no_mapping 标记 | 页面显示「⚠️未下户」徽标 ✅ |
| #8 | 备款口径定稿：备款=打款−(消耗+服务费)，退款不扣、凭证单独展示；删除矛盾注释 | 代码注释统一 ✅ |

修复后虚构演示数据已正式导入后端（与真实数据并存，页可直接查看）：
- 下户表 12 条（6 真实 + 6 模拟段，含双段账户 ACC-M1）
- 账户汇总出现：模拟户A1/A2（赊账负备款 ⚠️-142.69/-58.19）、模拟户M1 两行拆分、幽灵户（⚠️未下户徽标）
- 充值 84 / 打款 6 / 消耗 79 / 退款 3（各含模拟数据）
- 客户「丙公司」现金退款 50 后状态=已终止（refunded）

## 六、遗留事项（口径已定，待业务确认的行为）

1. 区间外消耗仍是"尾段兜底"策略（已有 overdue 标记+弹窗提示）——如需改为"拒绝导入"请告知
2. 未下户账户充值仍是软规则（已加徽标）——如需硬拒绝请告知


---

# 附录二 · 2026-09-06 增量复验（v2.5 → v3.8）

## A. 转户（按日期变更归属客户）数据链路复核
方法：以 `data/*.json` 原始行为输入，**独立实现**「日期→归属段→客户」规则（含空窗 gap_rule 与首段前兜底），再与系统各层逐行比对。规模：下户段 14（多段账户 2：ACC-M1、ACC-TRANSFER-1）· 消耗 84 行 · 充值 86 · 打款 9 · 退款 10 · gap_rule=prev。

| 层 | 比对内容 | 结果 |
|---|---|---|
| 1 | 每行消耗的客户/媒体/费率/段边界归属 + 区间外标记 | **0 不符**（84 行全比对）|
| 2 | 行级对齐（日期+账户 的行数、Σ金额）与 API 一致 | 0 差异 |
| 3 | 客户维度 Σ消耗/Σ服务费 vs `stats.summary.client_consume_totals` | 7 客户 0 差异；Σ消耗 137,640.47 守恒 |
| 4 | 段级余额自洽（分摊打款 −段消耗 −段费 = balance）与段累计充值归属 | 14 段全对 |
| 5 | 空窗行 | 2 行（ACC-TRANSFER-1 8/21、ACC-A2 9/11）均正确归前段并标 overdue |
| 6 | 转户拆分可加性 | ACC-TRANSFER-1 乙550+甲100=650=该户总量 ✓；ACC-M1 甲120+乙260=380 ✓ |
| 7 | 充值按**充值日期**归属段 | 86 行 0 不符 |
| 8 | 打款分摊 | Σ池 123,300.00 = Σ各段分摊（逐客户可加、尾差归最大条目）|
| 9 | 客户角色可见性（123=乙+丙） | 可见 6 行 Σ1,200.00 与复算一致；跨客户泄漏 0；余额表越权 0 |

边界单测（直接调用 `pick_mapping_segment`）6 例全通过：重叠边界日归前段 / 空洞归前段 / 尾段外兜底 / 早于首段 / 无日期取尾段 / 段内精确命中。另在隔离副本上端到端验证 `gap_rule=prev↔last` 切换后：归属客户、费率、客户汇总均随规则正确重算。

**发现并修复的盲点**：日期**早于最早下户段**的消耗会被静默归给首段客户且无任何提示（转户/补录漏登旧段时最容易背错账）。现 `segOut=before|after` 双向留痕，明细行显示 `⚠️ 早于下户期` / `⚠️ 超出归属期`，导出同名字段，弹窗给出天数与建议动作；历史数据在读取时按已存段边界自动派生，无需重算。

## B. 恒等式复核（口径命名统一后）
- 充值净结余（独立复算）= 125,560.84 − 3,410.00 − 137,640.47 − 5,702.93 = **−21,192.56** ⇔ 首页 KPI −21,192.55（分位舍入差）✓
- 净备款（独立）= 123,300.00 − 143,343.40 = **−20,043.40** ⇔ Σ账户净备款 −20,043.40，**差 0.00** ✓
- 备注：期间总余额从 −19,492.55 变为 −21,192.55（差 1,700）系使用者 18:30–18:31 自行删除 3 笔充值（ACC-TRANSFER-1 $200、ACC-A1 $1,000、ACC-A2 $500），非系统错误。

## C. 安全排查清单（v3.8，均含实测证据）
| # | 问题 | 验证方式 | 结果 |
|---|---|---|---|
| 1 | 存储型 XSS（下拉 `<option>`、表格 innerHTML 未转义） | 写入 `<img src=x onerror=window.__z=1>` 后遍历 9 面板 + 全子页签 | 修复前：注入元素 9~16 且脚本执行；修复后：**0 注入元素、未执行、字面文本显示** |
| 2 | 请求体无上限（内存 DoS / 请求走私） | 裸 socket 发 20MB Content-Length | `413` + 断连，后续请求正常 |
| 3 | PUT 字段注入 | PUT 携带 createdBy/attachments/id/createdAt | 金额生效，其余字段全被丢弃；定向更新不误伤他行 |
| 4 | 备份/恢复路径穿越 | 包内 `../users.json`、`uploads/payment/../../../etc/passwd` | 均 400 拒绝，users.json 未被覆盖 |
| 5 | 凭证越权读取 | company 角色取他人凭证 / 未登录取图 | 403 / 401 |
| 6 | 权限矩阵外泄 | 未登录访问 `/api/auth/roles` | 401（登录后 200）|
| 7 | 邮箱备份失败 | 本地复现旧写法 `quote() ... for bytes`；新写法头生成通过 | 已修（ASCII 附件名）|
| 8 | 凭证不入备份（换机丢附件） | 隔离副本：3 笔带凭证 → 备份 → 清空 → 恢复 | 打款数 = 记录附件数 = 磁盘文件数，图片 200 可读 |
| 9 | 半截 JSON / 并发写 | `save_json` 唯一临时名 + 写锁 + fsync | 语法与读取一致性保持 |
| 10 | 无界会话表 / 异常堆栈外泄 | 登录回收 + 上限 500；四 method 统一 500 wrapper | 生效 |
| 11 | `Vary: Accept-Encoding` 缺失（gzip 协商串味） | GET 头核对 | 静态与 JSON 均带 Vary；凭证带 `Vary: Cookie` |

## D. 本轮 UI 回归（v3.5 → v3.8）
- 三档宽度（1580 / 1000 / 640/390）定位记录后侧栏 `top=0`、模块 11/11 可点、无横向/纵向意外溢出
- 充值清零：账号筛选（下拉/搜索/计数/重置）与列头四维筛选叠乘；打款管理筛选条同时驱动明细与备款页签；计数胶囊 = 真实渲染行数
- 账号对账：列序与公式正确，逐客户净备款与备款一览 **0 差异**，屏蔽渠道角色列对齐 9/9/9 与 10/10/10
- 打款/退款：凭证上传（4 种格式真实解码）、大图预览、单张删除、12 张上限、伪装/超大拒绝，金额零影响
- 全程 JS 错误 0；测试产生的临时数据、凭证文件与审计噪声已全部回滚（打款 9 Σ123,300.00 / 退款 10 / 下户段 14 / 充值 86 Σ125,560.84）

## E. 遗留待决策
1. `script-src 'unsafe-inline'` 收紧（需事件委托 + nonce 改造）
2. 备份包是否默认排除 `users.json`（现含 pbkdf2 哈希、不含明文与会话令牌）


---

# 附录三 · v3.9 行内状态修改与表头筛选复验（2026-09-06）

## A. 充值明细「状态」行内修改
| 用例 | 结果 |
|---|---|
| 状态列为可行内下拉（🟢正常/🟡挂户/⚪清零），交互对齐下户表 | 选项与当前值读取正确（ACC-C1：正常）✓ |
| 改为「挂户」 | 充值行显示挂户，且 `mapping.json` 中该**归属段** status=挂户 ✓；再改回「正常」后段状态还原 ✓（数据无残留） |
| 数据模型一致性 | 充值流水 `status` 字段冗余数 = **0**（状态只存下户段，读取时按 periodStatus 派生）✓ |
| 未登记归属段的账户（ACC-X9） | 后端拒绝并提示「该账户在充值日期 2026-09-08 没有归属段，请先在下户表登记归属」；前端回读后下拉仍为「正常」（无假改）✓ |
| 并发/锁安全 | 助手 `_apply_account_status_locked()` 由调用方持锁（`threading.Lock` 不可重入，早期写法自死锁已消除）；复测期间接口无挂起 ✓ |

## B. 打款 / 退款 表头客户筛选
- 打款管理：表头选「环球广告」→ 明细 2 行、仅该客户、筛选条同步、备款余额子表同步收敛 1 行、胶囊 `打款 2 / 9 条 · 备款 1 / 8 个客户`；⟲重置恢复 9 行 ✓
- 退款管理：表头选「丙公司」→ 1 行；**财务影响合计 −50.00 = 可见行之和 −50.00** ✓；选无退款记录客户 → 显示「🔒 客户「星辰出海」暂无退款记录」且不残留合计行 ✓；还原 10 行 ✓
- 全程 JS 错误 0；账本基线未变：充值 86 条 Σ125,560.84 / 打款 9 条 Σ123,300.00 / 退款 10 条 / 下户段 14

## C. 交付物
本地包 `FB广告账户管理系统-本地版-v3.9.zip`（解压目录 `FB广告账户管理系统/`），含四份文档、全部 JSON 数据、转账凭证与最近 3 份备份；已用**解压后的副本实跑**验证（首页 200、登录、状态下拉写回归属段、凭证可读）。
