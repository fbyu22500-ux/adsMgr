# FB 广告账户管理系统 · 数据库设计（v1.0）

> 目标：在保持现有接口/前端零改动的前提下，给出从 JSON 文件存储 → SQLite（可平滑升级 MySQL/PG）的关系型设计，进一步压缩响应时间并支撑数据量增长。

## 1. 现状与已完成优化

当前 data/*.json + 进程内缓存：

| 优化 | 效果 |
|---|---|
| 文件原文缓存（全局版本号 + 磁盘 stat 双校验） | /api/mapping 14.5ms → 1.7ms（-88%） |
| 统计结果版本化缓存（(数据版本,用户,区间) 为键，写即失效） | /api/consume/stats 56.5ms → 1.7ms（-97%） |
| ETag + If-None-Match 协商缓存（no-cache，内容变才拉新） | 重复请求 304 零传输 |
| gzip（HTML / JS / 大 JSON） | xlsx 库 881KB → 316KB；首页传输 -68% |
| 会话 lastSeen 落盘节流（>60s 才写）；刷新匹配无变更不写盘 | 消除每请求写盘导致缓存永远失效 |

JSON 文件即“表”的映射（迁移数据源）：

`mapping.json→account_segment`、`consume.json→daily_spend`、`recharge.json→recharge`、`payment.json→payment`、`refund.json→refund`、`users.json→app_user`、`sessions.json→app_session`、`client_status.json→client_status`、`ops_log.json→ops_log`、`import_log.json→import_log`、`settings.json→app_setting`。

## 2. 概念模型（ER）

```
client（客户） 1───N account_segment（账户归属段） N───1 ad_account（广告账户, 逻辑实体=accountId）
                                                     │
        daily_spend（日消耗） ─── accountId + 日期命中 account_segment（归属计算）
        recharge（充值/清零） ── accountId + 充值日期命中段 → period_client（报表分组键）
        payment（打款, client 级）→ 按 client 池分摊到 account_segment 条目
        refund（退款, client 级）→ 不改变客户状态（v3.1 起：退款≠终止合作，同一客户可反复退款）
        ops_log（操作审计）：who/when/what/detail，所有写操作埋点
```

## 3. 表结构与索引（DDL · SQLite 方言）

### 3.1 主数据

```sql
CREATE TABLE client (
  code TEXT PRIMARY KEY,                -- 客户编码（名称唯一键，现数据用名称）
  name TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'active',-- active / refunded（历史遗留值；v3.1 起不再因退款写入，仅审计留档）
  refunded_at TEXT, refund_id TEXT, remark TEXT, operator TEXT
);

CREATE TABLE ad_account (
  account_id TEXT PRIMARY KEY,          -- BM 广告账号ID（业务主键，导入匹配键）
  name TEXT, channel TEXT, platform TEXT,-- 档案属性；以"最新生效段"冗余便于列表页直查
  first_seen TEXT, updated_at TEXT
);

CREATE TABLE app_user (
  id TEXT PRIMARY KEY, username TEXT UNIQUE NOT NULL, pass_hash TEXT NOT NULL,
  name TEXT, role TEXT NOT NULL DEFAULT 'company',
  client_code TEXT,                     -- FK client.code；客户角色必填（绑定客户）；NOT NULL 视角色
  nav TEXT,                             -- NULL=全部模块；JSON 数组
  scope TEXT,                           -- JSON: {clients:[],platforms:[],channels:[]}
  hide_channel INTEGER DEFAULT 0,
  active INTEGER DEFAULT 1, created_at TEXT,
  FOREIGN KEY (client_code) REFERENCES client(code)
);

CREATE TABLE app_session (
  token TEXT PRIMARY KEY, user_id TEXT NOT NULL REFERENCES app_user(id),
  created_at REAL, last_seen REAL, ip TEXT
);
```

### 3.2 事实表

```sql
CREATE TABLE account_segment (          -- 下户表：账户归属区间（多段=1 账户 N 行）
  id TEXT PRIMARY KEY,
  account_id TEXT NOT NULL REFERENCES ad_account(account_id),
  client_code TEXT NOT NULL REFERENCES client(code),   -- ★ 必填（与录入校验一致）
  start_date TEXT NOT NULL,             -- 下户日期（段起点）
  end_date TEXT,                        -- 归属截止（NULL=永久；段间重叠时前段自动截断至后段前一日）
  rate REAL DEFAULT 0,                  -- 服务点（0.03=3%）
  status TEXT NOT NULL DEFAULT '正常',  -- 正常/挂户/清零
  channel TEXT, platform_channel_no TEXT, note TEXT,
  created_by TEXT, created_at TEXT, updated_by TEXT, updated_at TEXT,
  UNIQUE (account_id, start_date)       -- 幂等 upsert 键（防同账户同日期重复段）
);

CREATE TABLE daily_spend (              -- BM 日消耗（导入只增改不删；未登记 account_id 拒删）
  account_id TEXT NOT NULL, day TEXT NOT NULL,          -- (account_id, day) 唯一
  spend REAL NOT NULL,
  client_code TEXT NOT NULL,            -- 按 day ∈ 段区间落库（★归属计算结果，冗余=查询免 JOIN）
  platform TEXT, channel TEXT, rate REAL, overdue INTEGER DEFAULT 0,
  imported_at TEXT, updated_at TEXT,
  PRIMARY KEY (account_id, day),
  FOREIGN KEY (account_id) REFERENCES ad_account(account_id)
);

CREATE TABLE recharge (                 -- 充值/清零流水（充值日期决定归属段）
  id TEXT PRIMARY KEY, account_id TEXT NOT NULL, day TEXT NOT NULL,
  client_code TEXT NOT NULL,            -- ★ by-day 段客户（period_client），非账户当前客户
  amount REAL DEFAULT 0, "clear" REAL DEFAULT 0, platform TEXT,
  created_by TEXT, created_at TEXT, updated_by TEXT, updated_at TEXT
);

CREATE TABLE payment (                  -- 打款（客户级）
  -- v3.1 起含 attachments 字段（JSON 数组），文件实体在 data/uploads/payment/<id>/
  id TEXT PRIMARY KEY, client_code TEXT NOT NULL REFERENCES client(code),
  day TEXT NOT NULL, amount REAL NOT NULL CHECK (amount != 0),
  method TEXT, note TEXT,
  created_by TEXT, created_at TEXT, updated_by TEXT, updated_at TEXT
);

CREATE TABLE refund (                   -- 退款/平账（客户级）
  id TEXT PRIMARY KEY, client_code TEXT NOT NULL REFERENCES client(code),
  day TEXT NOT NULL, amount REAL NOT NULL,
  type TEXT DEFAULT 'cash',             -- cash=现金退（仅冲减可退额度）/ offset=平账补差
  attachments TEXT                  -- JSON 数组：转账凭证（见 §9）
  method TEXT, note TEXT, operator TEXT, created_by TEXT, created_at TEXT
);

CREATE TABLE ops_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT, ts TEXT, uid TEXT, uname TEXT, role TEXT,
  module TEXT, action TEXT, detail TEXT, ip TEXT
);

CREATE TABLE import_log (
  id INTEGER PRIMARY KEY AUTOINCREMENT, ts TEXT, file TEXT, raw_rows INT, added INT,
  updated INT, rejected INT, media_mismatch INT, rejected_ids TEXT, mismatched TEXT, uid TEXT
);
```

### 3.3 视图（分摊/统计口径下沉）

备款=打款−(消耗+服务费) 的**分摊规则**（客户池→按条目占比、尾差进最大条目）用一张派生视图承载：

```sql
CREATE VIEW v_client_pool AS              -- 每客户打款池（区间参数化在应用层传入）
  SELECT c.code client_code, SUM(p.amount) pool
  FROM client c LEFT JOIN payment p ON p.client_code=c.code
                 AND p.day BETWEEN :from AND :to
  GROUP BY c.code;

CREATE VIEW v_entry_sf AS                 -- 条目（账户×段客户）消耗+服务费
  SELECT s.account_id, s.client_code,
         SUM(ds.spend) spend, SUM(ds.spend*s.rate) fee
  FROM account_segment s JOIN daily_spend ds
    ON ds.account_id=s.account_id
   AND ds.day BETWEEN s.start_date AND IFNULL(s.end_date,'9999-12-31')
  GROUP BY s.account_id, s.client_code;

-- 应用层再执行：alloc = pool * sf/total_sf（ROUND 2 位），尾差并入最大条目，得 t_alloc
```

## 4. 索引清单（与接口查询一一对应）

| 表 | 索引 | 服务的查询 |
|---|---|---|
| account_segment | UNIQUE(account_id,start_date)；idx(account_id,start_date,end_date) | 归属段命中 pick()；幂等 upsert |
| daily_spend | PK(account_id,day)；idx(day)；idx(client_code,day) | 按账户明细/按客户/区间过滤 |
| recharge | idx(account_id,day)；idx(client_code,day) | 期间归属、余额累计、客户范围过滤 |
| payment | idx(client_code,day) | 客户池 |
| refund | idx(client_code,day) | 净备款 |
| ops_log | idx(ts)；idx(module,ts) | 操作记录倒序分页 |
| app_session | idx(user_id)、PK(token) | 令牌鉴权 O(1) |
| app_user | UNIQUE(username)、idx(role,status) | 登录、账号列表 |

**通用要求**：所有列表接口一律 `LIMIT/OFFSET` 或 keyset 分页——与前端 50/100/500 分页一一对应；列表响应只 SELECT 渲染需要列。

## 5. 一致性与写入规则（迁移时保留）

1. 下户表：client+account_id 必填；(account_id,start_date) 幂等 upsert，只覆盖同键段，**保留历史段**；段重叠自动截断（前段结束=后段开始−1 日）。
2. BM 导入：accountId 无段→拒绝；Excel 媒体≠段媒体（段媒体为空则放行）→拒绝；(account_id,day) 命中→更新，否则新增；day>段end→overdue=1。
3. 空窗期归属：`gap_rule`（默认 prev；=设置项）。
4. 充值：按【充值日期】所属段写 client_code（period）；批量/导入同规则；服务端写 created_by/created_at，客户端不可伪造。
5. 现金退款（v3.1 修订）：**不再写 client.status**；「可退备款 = 累计打款 −（消耗 + 服务费）− 已退现金」，同客户可重复退款；删除打款/退款记录（含凭证删除）需 `can_delete_money`（管理员、财务），删除后所有派生值按剩余记录重算。
6. 所有写操作：业务表 + ops_log **同事务**（SQLite BEGIN IMMEDIATE；后续 MySQL 用事务+redo log）。
7. 写后失效：迁移后保留“数据版本”概念：`meta.version++`，进程内 stats 缓存按其失效（或改物化汇总表 t_stats_cache）。

## 6. 迁移路径（三阶段，零停服风险）

- **阶段 1（当前已落地）**：缓存层（原文缓存 + 结果缓存 + ETag/gzip），JSON 为权威存储；统计已 ≤2ms。
- **阶段 2**：引入 SQLite（单文件 `data/app.db`），Python 侧封装 4 个 repository（load/save/query/aggregate）替代 load_json/save_json 调用点；用一次性导入脚本把 JSON 灌入（保留 id 与时间戳）；灰度开关：读走 DB、写双写→读双读 diff 24h→切换权威；ETag 仍生效。
- **阶段 3**：分析型需求增长时按同一 Schema 平滑迁 MySQL（加 `PARTITION BY` day 到月，daily_spend 分区），接口层不变。

## 7. 响应预算（验收指标）

| 接口 | 现状（JSON+缓存） | SQLite 目标 | MySQL 目标 |
|---|---|---|---|
| GET /api/mapping | 1.7ms | ≤5ms（LIMIT 分页） | ≤10ms |
| GET /api/consume（84 行） | 1-2ms | ≤8ms/页50 | ≤10ms |
| GET /api/consume/stats | 1.7ms（热）/ 6ms（首次） | ≤15ms（首次聚合） | ≤40ms |
| POST /api/mapping（含重匹配） | ~8ms | ≤30ms | ≤50ms |
| 导入 200 行 BM | ~90ms | ≤300ms（批量 upsert） | ≤500ms |
| 首页加载（gzip 后 JS 316KB+页面） | ~1s 内网 | 同左（gzip+304） | 同左 |
| 带宽 | 较原始 -68%（xlsx 全量 881→316KB） | 同左 + 304 零 body | 同左 |

## 8. 安全（保留已落地项）

API 浏览器标识闸 + 每 IP 600 次/分限流（多图上传需要，登录爆破另有独立锁定）、登录 5 次锁 15 分钟（后续改按 user 维 + 验证码）、路径穿越拦截、CSP/X-Frame-Options/nosniff/X-Robots-Tag、robots 全站禁止、Server 指纹中性化、备份包剔除 sessions.json、录入人服务端强制写入、密码 PBKDF2-100k、会话 7 天滑动过期+60s 落盘节流。迁移后新增：SQL 一律参数化；连接串/授权码只放 settings 表并加密（应用层 Fernet，密钥走环境变量）。


## 9. 转账凭证（attachments）存储设计 · v3.1 起
**记录内元数据**（`payment.attachments` / `refund.attachments`，JSON 数组）：
```json
{ "id": "1788686842408_6b9dcb", "file": "1788686842408_6b9dcb.png", "mime": "image/png",
  "size": 13191, "name": "UI3_03.png", "uploadedBy": "超级管理员", "uploadedAt": "2026-09-06 17:27:22" }
```
**物理落盘**：`data/uploads/<payment|refund>/<记录id>/<attId><ext>`
| 约束 | 值 |
|---|---|
| 类型判定 | 按**文件头魔数**（png/jpg/jpeg/gif/webp），不信客户端 MIME 与扩展名 |
| 单张 / 单笔 | ≤8MB / ≤12 张；请求体总上限 16MB（413） |
| 读取路径 | 仅 `GET /attachments/<kind>/<记录id>/<attId>`：需登录 + 该笔账在调用者数据范围内可见（客户角色取不到他人凭证，实测 403）；`Cache-Control: private` + `Vary: Cookie` |
| 静态隔离 | `data/` 一律禁止静态直读（`/data/payment.json`、`/data/uploads/...` 均 404）|
| 级联清理 | 删除记录 / 清空打款 / 一键清空 → 同步删除其凭证目录；末张删除后回收空目录 |
| 备份恢复 | 备份包内含 `uploads/**`；恢复时逐文件 `realpath` 校验必须落在 `data/uploads/` 内，且限图片扩展名（防穿越）|
| 迁移到 SQLite 后 | 建议 `attachment(id, doc_kind, doc_id, file_name, mime, size, created_by, created_at)` 表 + `BLOB` 或对象存储路径；元数据表与凭证文件分离，便于按笔核验 |

## 10. 口径术语表（v3.6 统一命名，避免三处都叫"余额"）
| 字段 | 公式 | 出现位置 |
|---|---|---|
| 充值净结余 | 累计充值 − 累计清零 − 累计消耗 − 累计服务费 | 首页 KPI、日消耗弹窗 |
| 净备款 | 累计打款 −（累计消耗 + 累计服务费） | 打款管理、备款一览（客户维度） |
| 广告账号余额 | 该账户累计充值 − 累计消耗（不扣清零与服务费） | 按账号汇总末列；净备款（打款口径）见备款一览 |
| 投放结余 | 累计充值 − 累计消耗 | 账号对账 |
| 差额 | 投放结余 − 累计清零 | 账号对账 |
| 账户结余 | 累计充值 − 累计清零 | 充值明细 |
| 可退备款 | 累计打款 −（消耗 + 服务费）− 已退现金 | 退款录入联动 |

## 11. v3.8 安全加固对写入/接口的影响
- 编辑类 PUT（payment / recharge / mapping）实施**字段白名单**，`id/createdBy/createdAt/attachments` 不可由外部改（attachments 只能经专用上传接口写）。
- `save_json`：唯一临时文件名 + 全局写锁 + fsync + `os.replace` → 并发/断电不产生半截 JSON；`_DATA_VERSION` 递增使 RAW/STATS 缓存即时失效。
- 四个 HTTP 入口统一异常兜底：内部错误只回 `500 {"error": ...}`，不外泄堆栈、不留半截响应；`_read_body` 超限返回 `413` 并断连（防请求走私）。
- 全站 HTML 输出转义（表格单元格、下拉 `<option>`、内嵌 onclick 参数 `escJ`）→ 账户ID/客户名/备注等自由文本不再有脚本执行面。
- `/api/auth/roles` 需登录；`sessions.json` 登录时回收过期项并限 500 条。


## 12. 账户状态的唯一归属（v3.9 明确）
- 状态字段（正常/挂户/清零）**只存在 `mapping`（下户归属段）上**；`recharge` 行不存 status（历史冗余字段已停用，读取时按命中段派生为 `periodStatus`）。
- 两个写入口共用一条通路 `_apply_account_status_locked(account_id, 充值日期, 新状态, 操作人)`：
  1. 充值录入表单的「账户状态」下拉（POST /api/recharge，带 status 时先写段再落流水）；
  2. 充值明细行内「状态」下拉（PUT /api/recharge/<id>，v3.9 新增）。
- 命中规则与消耗一致：`pick_mapping_segment(账户ID, 充值日期)`；未命中任何段 → `400` 并提示先到下户表登记；命中 → 写段 + `_refresh_consume_matching()` 重跑全部消耗归属，因此**一次修改会同时纠正该行所代表时段的客户归属与统计**。
- 并发约束：`_lock` 不可重入，故该助手为"调用方持锁"版本（内部不再 `with _lock`）；POST 侧自行 `with _lock` 包一层。
